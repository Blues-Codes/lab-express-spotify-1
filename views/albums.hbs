<h1>Albums for: {{items.0.artists.0.name}}</h1>

{{#each items}}

{{#if images}}

<img src="{{images.0.url}}" alt="">
    {{/if}}
<h1>{{name}}</h1>

<a href="/albums/{{id}}">View Tracks</a>

{{/each}}